# Research: Norato Textarea

- Native `textarea` is the canonical semantic primitive.
- Fixed height with internal scrolling gives predictable form layout and matches the accepted clarification.
- Reuse shadcn-style composition only where compatible; Norato owns tokens, RTL, states, and typography.
