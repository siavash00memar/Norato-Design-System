# Norato Textarea Contract

Textarea is a separate multiline control. It has a persistent label, optional helper/error messages, default RTL direction, fixed height, internal scrolling, minimum 48px target, semantic invalid state, and no auto-grow behavior.
