# Local Quickstart

Open `site/index.html` directly in a browser, or serve the repository with any static HTTP server. Use the sidebar on desktop and bottom navigation on compact widths. Vercel deployment is intentionally deferred until local approval.
