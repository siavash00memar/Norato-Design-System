# Norato Button Contract

Text Button and Icon-only Button are separate public contracts. Both use a semantic button, default to RTL-compatible content direction, use Norato semantic tokens, and preserve a minimum 48px target. Icon-only requires an explicit accessible name. Loading prevents duplicate activation and exposes progress semantics.
