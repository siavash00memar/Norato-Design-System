# Quickstart: Norato Button

Render a text Button and an Icon-only Button in HTML/CSS, Vue, and Next.js. Review every intent and state, keyboard focus, form type, loading prevention, RTL alignment, and accessible name. Then run TypeScript and token validation.
