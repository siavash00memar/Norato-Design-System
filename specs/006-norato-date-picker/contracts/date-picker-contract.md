# Norato Date Picker Contract

Single Jalali date. The closed control is a labeled text-like field with a calendar affordance and Figma-derived styling. Open state and calendar semantics follow Material Design. `value` is a Jalali date string; range selection is out of scope.
