# Data Model: Norato Date Picker

`id`, `label`, `value` (Jalali date string), `placeholder`, `helperText`, `errorText`, `disabled`, `loading`, `open`, and `dir`.

Scope is one date only. The calendar exposes selected, focused, unavailable, and today states.
