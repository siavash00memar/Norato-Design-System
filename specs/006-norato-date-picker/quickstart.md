# Quickstart

Render the Figma-matched closed field, calendar affordance, states, and one-date Jalali value in HTML/CSS, Vue, and Next.js. Review keyboard semantics, RTL, error association, and minimum target, then run TypeScript and token validation.
