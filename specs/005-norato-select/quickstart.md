# Quickstart

Render native Select states in HTML/CSS, Vue, and Next.js. Review RTL, keyboard and mobile behavior, label/error association, empty options, disabled options, and minimum target. Run TypeScript and token validation.
