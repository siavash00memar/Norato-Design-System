# Data Model: Norato Select

`id`, `label`, `options`, `value`, `placeholder`, `helperText`, `errorText`, `disabled`, `loading`, `required`, and `dir`.

The native control owns open, keyboard, and selected-state behavior. Loading and disabled prevent selection.
