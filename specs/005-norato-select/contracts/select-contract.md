# Norato Select Contract

Select is a native single-choice control with persistent label, options, optional placeholder/helper/error, RTL default, explicit direction override, minimum 48px target, and semantic invalid/disabled/required behavior.
