# Research: Norato Select

- Native HTML select is the canonical primitive for mobile compatibility, keyboard support, and baseline semantics.
- Norato controls the wrapper, tokens, typography, RTL, and validation messaging.
- Custom listbox behavior is deferred to a future explicitly specified component.
