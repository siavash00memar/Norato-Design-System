# Quickstart

Open `docs/index.html` for framework-neutral documentation. Run Storybook using the project’s configured Storybook command when dependencies are added; stories under `stories/` are the canonical state inventory.
