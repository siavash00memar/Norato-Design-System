# Documentation Contract

Every implemented component must appear in the index with a contract link, supported adapters, state list, RTL note, and accessibility note. Future components must not be presented as implemented.
