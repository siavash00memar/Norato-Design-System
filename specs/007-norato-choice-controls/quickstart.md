# Quickstart

Render Checkbox and Radio groups in HTML/CSS, Vue, and Next.js. Review RTL, keyboard order, selected/disabled/error states, group semantics, and 48px targets. Run TypeScript and token validation.
