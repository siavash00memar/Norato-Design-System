# Research: Norato Choice Controls

- Native checkbox and radio inputs are canonical for keyboard and assistive technology behavior.
- Checkbox supports multiple values; radio supports one value per group.
- Norato owns visual styling while preserving native semantic input behavior.
