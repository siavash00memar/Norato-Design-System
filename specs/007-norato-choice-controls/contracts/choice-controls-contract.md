# Choice Controls Contract

Checkbox Group and Radio Group are separate contracts. Checkbox accepts multiple selected values; Radio accepts exactly one. Both expose group labels, native option semantics, RTL direction, error association, and minimum targets.
