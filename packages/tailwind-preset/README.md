# Norato Tailwind Preset

Import the preset and load `packages/tokens/src/index.css`. Utilities map to semantic CSS variables;
do not redefine Norato token values in page-local Tailwind configuration.
