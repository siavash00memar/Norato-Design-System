# Norato HTML/CSS

Load `packages/tokens/src/index.css` and `packages/core-html/src/norato.css`. Set `dir="rtl"` on
the document or relevant root. Use semantic classes and keep the required 48px interaction target.
