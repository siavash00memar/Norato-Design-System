# Norato Field

`noratoFieldMarkup` is the canonical HTML/CSS single-line text Field. It keeps the label persistent, associates helper/error messages through IDs, defaults to RTL, and uses Norato semantic tokens. Textarea, Select, and Date Picker are separate components.
