<script setup lang="ts">defineProps<{ id: string; label: string; modelValue?: string; placeholder?: string; helperText?: string; errorText?: string; disabled?: boolean; loading?: boolean; dir?: 'rtl' | 'ltr' | 'auto' }>();</script>
<template><div class="norato-date-picker" :class="{ 'is-invalid': errorText, 'is-loading': loading }"><label class="norato-date-picker__label" :for="id">{{ label }}</label><div class="norato-date-picker__control"><input class="norato-date-picker__input" :id="id" :dir="dir ?? 'rtl'" :value="modelValue" :placeholder="placeholder ?? 'تاریخ را انتخاب کنید'" readonly :disabled="disabled || loading" :aria-invalid="errorText ? 'true' : undefined" /><button class="norato-date-picker__trigger" type="button" aria-label="باز کردن تقویم" aria-haspopup="dialog" aria-expanded="false" :disabled="disabled || loading">▣</button></div><p v-if="helperText" class="norato-date-picker__message">{{ helperText }}</p><p v-if="errorText" class="norato-date-picker__message norato-date-picker__error" role="alert">{{ errorText }}</p></div></template>
