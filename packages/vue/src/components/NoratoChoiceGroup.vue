<script setup lang="ts">defineProps<{ kind: 'checkbox' | 'radio'; id: string; label: string; options: { value: string; label: string; disabled?: boolean }[]; selected?: string[]; errorText?: string }>();</script>
<template><fieldset class="norato-choice-group" dir="rtl"><legend>{{ label }}</legend><label v-for="option in options" :key="option.value" class="norato-choice"><input :type="kind" :name="id" :value="option.value" :checked="selected?.includes(option.value)" :disabled="option.disabled" /><span>{{ option.label }}</span></label><p v-if="errorText" class="norato-choice__error" role="alert">{{ errorText }}</p></fieldset></template>
