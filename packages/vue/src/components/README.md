# Vue adapters

Vue adapters consume the framework-neutral Norato token and component contracts. Representative
Button and Field adapters are reserved for the next implementation slice after package setup.
