# Norato Next.js Adapter

Next.js components consume the same CSS token entrypoint and preserve the framework-neutral contract.
Use `NoratoButton` and `NoratoField` as the representative adapter examples.
