type Props = { id: string; label: string; options: { value: string; label: string; disabled?: boolean }[]; value?: string; placeholder?: string; helperText?: string; errorText?: string; disabled?: boolean; loading?: boolean; required?: boolean; dir?: 'rtl' | 'ltr' | 'auto' };
export function NoratoSelect({ id, label, options, value, placeholder, helperText, errorText, disabled = false, loading = false, required = false, dir = 'rtl' }: Props) { return <div className={`norato-select-group${errorText ? ' is-invalid' : ''}${loading ? ' is-loading' : ''}`}><label className="norato-select__label" htmlFor={id}>{label}{required && <span aria-hidden="true"> *</span>}</label><select className="norato-select" id={id} dir={dir} value={value} disabled={disabled || loading} required={required} aria-invalid={errorText ? 'true' : undefined}>{placeholder && <option value="">{placeholder}</option>}{options.map(option => <option key={option.value} value={option.value} disabled={option.disabled}>{option.label}</option>)}</select>{helperText && <p className="norato-select__message">{helperText}</p>}{errorText && <p className="norato-select__message norato-select__error" role="alert">{errorText}</p>}</div>; }
