# Next.js adapters

Next.js adapters consume the same framework-neutral Norato token and component contracts as Vue and
HTML/CSS. Representative adapters are reserved for the next implementation slice after package setup.
