type Props = { id: string; label: string; value?: string; placeholder?: string; helperText?: string; errorText?: string; disabled?: boolean; loading?: boolean; dir?: 'rtl' | 'ltr' | 'auto' };
export function NoratoDatePicker({ id, label, value, placeholder = 'تاریخ را انتخاب کنید', helperText, errorText, disabled = false, loading = false, dir = 'rtl' }: Props) { return <div className={`norato-date-picker${errorText ? ' is-invalid' : ''}${loading ? ' is-loading' : ''}`}><label className="norato-date-picker__label" htmlFor={id}>{label}</label><div className="norato-date-picker__control"><input className="norato-date-picker__input" id={id} dir={dir} value={value} placeholder={placeholder} readOnly disabled={disabled || loading} aria-invalid={errorText ? 'true' : undefined} /><button className="norato-date-picker__trigger" type="button" aria-label="باز کردن تقویم" aria-haspopup="dialog" aria-expanded="false" disabled={disabled || loading}>▣</button></div>{helperText && <p className="norato-date-picker__message">{helperText}</p>}{errorText && <p className="norato-date-picker__message norato-date-picker__error" role="alert">{errorText}</p>}</div>; }
