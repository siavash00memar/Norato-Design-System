type Props = { id: string; label: string; placeholder?: string; value?: string; helperText?: string; errorText?: string; disabled?: boolean; loading?: boolean; required?: boolean; dir?: 'rtl' | 'ltr' | 'auto' };
export function NoratoTextarea({ id, label, placeholder, value, helperText, errorText, disabled = false, loading = false, required = false, dir = 'rtl' }: Props) { const describedBy = [helperText && `${id}-helper`, errorText && `${id}-error`].filter(Boolean).join(' ') || undefined; return <div className={`norato-textarea-group${errorText ? ' is-invalid' : ''}${loading ? ' is-loading' : ''}`}><label className="norato-textarea__label" htmlFor={id}>{label}{required && <span aria-hidden="true"> *</span>}</label><textarea className="norato-textarea" id={id} dir={dir} value={value} placeholder={placeholder} disabled={disabled || loading} required={required} aria-invalid={errorText ? 'true' : undefined} aria-describedby={describedBy} />{helperText && <p className="norato-textarea__message" id={`${id}-helper`}>{helperText}</p>}{errorText && <p className="norato-textarea__message norato-textarea__error" id={`${id}-error`} role="alert">{errorText}</p>}</div>; }
