type Props = { kind: 'checkbox' | 'radio'; id: string; label: string; options: { value: string; label: string; disabled?: boolean }[]; selected?: string[]; errorText?: string };
export function NoratoChoiceGroup({ kind, id, label, options, selected = [], errorText }: Props) { return <fieldset className="norato-choice-group" dir="rtl"><legend>{label}</legend>{options.map(option => <label className="norato-choice" key={option.value}><input type={kind} name={id} value={option.value} defaultChecked={selected.includes(option.value)} disabled={option.disabled} /><span>{option.label}</span></label>)}{errorText && <p className="norato-choice__error" role="alert">{errorText}</p>}</fieldset>; }
