# الگوهای واقعی مسیر شغلی نوراتو

این سند از فایل Figma مسیر شغلی، فایل `DFlMeVZgJPfpbfjTlNxMvI` و node `11731:23672` استخراج شده است.

## قاب موبایل

- عرض نمونه: `360px`
- gutter افقی صفحه: `24px`
- عرض محتوای اصلی: `312px`
- محتوای اصلی نباید از gutter عبور کند.

## نوار پیشرفت

- ارتفاع progress bar: `48px`
- در بالای flow قرار می‌گیرد.
- progress بخشی از context مرحله است، نه یک تزئین مستقل.

## پیام‌های محتوایی

- کارت پیام اصلی: `312px` عرض و نمونهٔ `136px` ارتفاع.
- padding داخلی مشاهده‌شده: `12px`.
- متن فارسی با line-height باز و قابل خواندن نمایش داده می‌شود.
- پیام باید بخشی از جریان مرحله باشد و با actionهای پایین صفحه رقابت نکند.

## Action Row

- ارتفاع کنترل‌های action: `36px`.
- gutter داخلی صفحه: `24px`.
- فاصلهٔ label و control در الگوهای Field: `6px`.
- Action اصلی در سمت راست visual flow و بازگشت در سمت مقابل قرار می‌گیرد.

## Field Group

- کارت گروهی سفید با shadow سبک.
- padding داخلی نمونه: `12px` تا `18px` بسته به سطح wrapper.
- فاصلهٔ بین title و field group: `8px`.
- fieldهای داخل group با ارتفاع `36px` و radius pill نمایش داده می‌شوند.

## دامنهٔ مشاهده

در node انتخاب‌شده Modal مشاهده نشد؛ قرارداد Modal تا دریافت node مشخص مربوط به Modal تعریف یا پیاده‌سازی نمی‌شود.
