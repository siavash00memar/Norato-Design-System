# Legacy Guide Decisions

The pasted legacy guide is retained as reference only. Its values are promoted only when compatible
with the approved Material authority, Figma-derived typography, RTL contract, and current tokens.
Conflicting legacy values are not silently imported.
